type ChangeStyles = {
  borderColor: string;
  position: number;
  fontSize: number;
  focus: string;
  name: string;
  borderWidth: number;
}[];

type InputValues = {
  Name: string;
  Email: string;
};

export type { ChangeStyles, InputValues };
